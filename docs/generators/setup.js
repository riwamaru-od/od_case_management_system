const C = require('./common');
const { h1, h2, h3, p, bullet, step, box, table, spacer, pageBreak, coverPage } = C;

const c = [];

c.push(...coverPage(
  '案件管理・見積書自動作成システム',
  '初期設定マニュアル v2.3',
  [
    ['文書名', '案件管理・見積書自動作成システム 初期設定マニュアル'],
    ['版数', 'v2.3'],
    ['作成日', '2026年9月4日'],
    ['対象読者', 'システム管理担当者'],
    ['前提知識', 'Googleスプレッドシート、Googleドライブ、Google Apps Scriptの基本操作'],
  ]
));

c.push(h1('目次'));
[
  '1. 事前準備', '2. 管理用アカウントの用意', '3. スプレッドシートの準備', '4. Driveフォルダの準備',
  '5. テンプレートファイルの準備', '6. 社印画像の準備', '7. スクリプトの配置',
  '8. Webアプリのデプロイ', '9. スクリプトプロパティの設定', '10. トリガーの設定と認可',
  '11. 動作確認チェックリスト', '12. 運用・保守', '13. トラブルシューティング',
].forEach(t => c.push(p('　' + t)));
c.push(pageBreak());

c.push(h1('1. 事前準備'));
c.push(p('設定を始める前に、以下をご用意ください。'));
c.push(table(
  ['項目', '内容'],
  [
    ['管理用アカウント', 'トリガー設定と、すべての書類作成を行う専用アカウント（第2章）'],
    ['社印画像', 'PNGまたはJPEG形式の画像ファイル（第6章）'],
    ['帳票テンプレート', '見積書・請求書・納品書のスプレッドシート（第5章）'],
    ['Driveフォルダ', '書類3種＋アーカイブ＋バックアップの計5フォルダ（第4章）'],
    ['Chatwork APIトークン', '総務以外の社員へ通知する場合に必要'],
  ],
  [2400, 6600]
));

c.push(h2('1.1 設定の全体像'));
[
  '管理用アカウントを用意する',
  'メインスプレッドシートを作成し、必要なシートを配置する',
  'Driveにフォルダ構成を作成する',
  '帳票テンプレートを配置する',
  '社印画像を配置する',
  'スクリプトを配置する',
  'Webアプリとしてデプロイする',
  'スクリプトプロパティを設定する（WebアプリのURLを含む）',
  'トリガーを設定し、認可を完了する',
  '動作確認を行う',
].forEach(t => c.push(step(t)));

c.push(h1('2. 管理用アカウントの用意'));
c.push(p('本システムは、生成した書類ファイルのオーナーを「管理用アカウント」に統一することで、承認済み書類が編集されることを防ぎます。'));

c.push(h2('2.1 オーナー統一の考え方'));
c.push(p('Google Workspace を利用しない環境では、スクリプトからファイルのオーナーを別アカウントへ移譲することができません（異なるドメイン間の移譲は「招待制」となり、相手が承諾するまで反映されないためです）。'));
c.push(p('そこで本システムでは、書類ファイルを作成する処理そのものを管理用アカウントの権限で実行することで、生成される全書類が最初から管理用アカウントの所有物になるようにします。'));
c.push(p('この「管理用アカウントの権限での実行」は、同じスクリプトをWebアプリとしてデプロイし、サイドバーからの書き込み処理をそこへ中継することで実現しています（第8章）。そのため、一般社員がサイドバーを操作した場合でも書類のオーナーは管理用アカウントになります。'));
c.push(spacer());
c.push(box('運用ルール（重要）', [
  '・Webアプリを「実行ユーザー: 自分」でデプロイするアカウントが、全書類のオーナーになります。',
  '　デプロイは必ず管理用アカウントで行ってください（第8章）。',
  '・Googleスプレッドシートの仕様上、ファイルのオーナーだけはシート保護を解除できます。',
  '　この制限はいかなる方法でも回避できません。',
  '・したがって、管理用アカウントの認証情報を日常業務で使用しないことが、書類保護の前提となります。',
  '・想定外の経路で別アカウントが書類を作成した場合は、「操作ログ」シートにエラーとして記録されます。',
], 'warn'));
c.push(spacer());
c.push(p('用意するもの：専用のGoogleアカウント1つ（例: system-admin@example.com）。'));

c.push(h1('3. スプレッドシートの準備'));

c.push(h2('3.1 シートの作成'));
c.push(p('メインスプレッドシートを1つ作成し、以下のシートを用意します。'));
c.push(table(
  ['シート名', '用意方法'],
  [
    ['原本_表示', '手動で作成（列構成は3.2参照）'],
    ['原本_全案件DB', '「原本_表示」と同じ列構成で作成'],
    ['{期}期_表示', '「原本_表示」をコピーして作成（例: 17期_表示）'],
    ['{期}期_全案件DB', '「原本_全案件DB」をコピーして作成（例: 17期_全案件DB）'],
    ['取引先DB', 'Googleフォームの回答先として作成'],
    ['社員DB', '手動で作成'],
    ['操作ログ', '不要（初回操作時に自動生成されます）'],
    ['config', '不要（初回の日次トリガー実行時に自動生成されます。3.3節参照）'],
  ],
  [2600, 6400]
));
c.push(spacer());
c.push(p('過去期のシートは自動的に別ファイルへ退避されるため、メインスプレッドシートには常に当期分のみが存在する状態になります（11.2節参照）。'));

c.push(h2('3.2 案件シートのヘッダー行'));
c.push(p('「原本_表示」「原本_全案件DB」「{期}期_表示」「{期}期_全案件DB」の4シートすべてに、以下のヘッダーを設定します。'));
c.push(spacer());
c.push(box('見出しの行がシートによって異なります', [
  '・「表示」系のシート … 1〜2行目が見出しで、案件データは3行目から始まります。',
  '　　列ごとの見出し（下表）は2行目に入ります。1行目は見出しの1段目としてご自由に使えます。',
  '・「全案件DB」系のシート … 1行目のみが見出しで、案件データは2行目から始まります。',
  '',
  'メニュー「管理者用メニュー > 案件シートの構成を確認・修復する」は、この違いを踏まえて',
  'それぞれ正しい行へ見出しを補います。',
]));
c.push(table(
  ['列', 'ヘッダー名', '列', 'ヘッダー名'],
  [
    ['A', '案件番号', 'Q', '見積書承認者'],
    ['B', '取引先名', 'R', '見積書承認日時'],
    ['C', '案件名', 'S', '見積書出力者'],
    ['D', '終了予定', 'T', '見積書出力日時'],
    ['E', '請求予定', 'U', '請求書作成者'],
    ['F', '担当', 'V', '請求書作成日時'],
    ['G', 'ステータス', 'W', '請求書承認者'],
    ['H', '請求ステータス', 'X', '請求書承認日時'],
    ['I', '見積書', 'Y', '請求書出力者'],
    ['J', '見積書(出力)', 'Z', '請求書出力日時'],
    ['K', '請求書', 'AA', '納品書作成者'],
    ['L', '請求書(出力)', 'AB', '納品書作成日時'],
    ['M', '納品書', 'AC', '最終承認者'],
    ['N', '納品書(出力)', 'AD', '最終承認日時'],
    ['O', '見積書作成者', 'AE', '見積書差し戻し日時'],
    ['P', '見積書作成日時', 'AF', '請求書差し戻し日時'],
    ['', '', 'AG', '見積書再承認待ち'],
    ['', '', 'AH', '請求書再承認待ち'],
    ['', '', 'AI', '見積書着手日時'],
    ['', '', 'AJ', '請求書着手日時'],
  ],
  [700, 3800, 700, 3800]
));
c.push(spacer());
c.push(box('システムが内部的に使用する列（AE〜AJ）', [
  'AE〜AJの6列は、システムが内部的に使用するもので、利用者が手入力する必要はありません。',
  '',
  '・AE / AF（差し戻し日時） … 「再作成」ボタンの活性判定と、差し戻し後の再送防止に使用',
  '・AG / AH（再承認待ち）　 … 差し戻し・再作成の後、再承認されるまで出力を止めるために使用',
  '・AI / AJ（着手日時）　　 … 作成中のまま放置された書類のアラート判定に使用',
  '',
  '旧版から移行する場合、メニュー「管理者用メニュー > 案件シートの構成を確認・修復する」を',
  '実行すれば、列数の拡張とヘッダーの設定が自動で行われます（3.5節参照）。',
  '列を追加した後は、シートの保護範囲もAJ列まで広げてください。',
], 'warn'));

c.push(h2('3.3 入力規則（プルダウン）の設定'));
c.push(table(
  ['列', '設定内容'],
  [
    ['B（取引先名）', '取引先DBの社名列を参照するプルダウン（手動設定）'],
    ['D（終了予定）', '「configシート」の選択肢一覧を参照するプルダウン（自動設定。下記参照）'],
    ['E（請求予定）', '同上'],
    ['F（担当）', '社員DBの氏名列を参照するプルダウン（手動設定）'],
  ],
  [2400, 6600]
));
c.push(spacer());
c.push(box('D列・E列は手動設定不要', [
  '終了予定・請求予定は「config」シートに自動生成される選択肢（今月を基準に1ヶ月前〜6ヶ月後の',
  '「YYYY/MM 前半」「YYYY/MM 後半」、および「未定」「営業案件」）を参照するプルダウンとして、',
  '日次トリガーが自動的に設定します。手動でプルダウンを組む必要はありません。',
  '',
  'あわせて、請求予定が今月分の行は自動的に文字色が青になります（条件付き書式）。',
  '選択肢の月範囲や固定選択肢、ハイライトの色は Constants.gs の SCHEDULE_OPTIONS_* /',
  'CURRENT_MONTH_BILLING_TEXT_COLOR で調整できます（11.5節参照）。',
]));

c.push(h2('3.4 社員DBの設定'));
c.push(table(
  ['列', '項目名', '設定内容'],
  [
    ['A', '社員番号', '任意'],
    ['B', '氏名', '案件シートの「担当」プルダウンと完全一致させること'],
    ['C', 'メールアドレス', '必須。ユーザー識別と通知に使用'],
    ['D', 'Chatwork Room ID', '総務ロール以外の社員に必要'],
    ['E', '部署', '任意'],
    ['F〜K', 'ロール1〜6', '下表のロール名を正確に入力（複数可）'],
  ],
  [800, 2400, 5800]
));
c.push(spacer());
c.push(table(
  ['ロール名', '権限'],
  [
    ['Admin', '全操作'],
    ['総務', '全操作 ＋ メール通知の宛先になる'],
    ['見積書承認', '見積書の承認・差し戻し・再作成'],
    ['請求書承認', '請求書の承認・差し戻し・再作成'],
    ['最終承認', '最終承認'],
    ['一般社員', '作成・完成・PDF出力・案件中止'],
  ],
  [2400, 6600]
));
c.push(spacer());
c.push(p('ロール名は上記の文字列と完全一致させてください。少なくとも1名に「総務」を設定する必要があります（承認依頼メールの宛先となるため）。'));

c.push(h2('3.5 シート構成の確認・修復'));
c.push(p('シートの列数やヘッダーが正しく設定されているかは、専用のメニューで確認・修復できます。列を追加する仕様変更を既存のスプレッドシートへ反映する際にも使用します。'));
[
  'メニュー「案件管理システム」→「案件シートの構成を確認・修復する」をクリックします',
  '当期の表示シート・全案件DBシートと、原本の2シートが対象になります',
  '列数が不足していれば自動的に拡張され、ヘッダーが空欄の列に既定の見出しが設定されます',
  '結果がダイアログに表示され、「操作ログ」シートにも記録されます',
].forEach(t => c.push(step(t)));
c.push(spacer());
c.push(box('既存のヘッダーは上書きされません', [
  '既に見出しが入力されている列は変更されません（独自の表記に変更している場合を尊重するため）。',
  '空欄の列にのみ、既定の見出しが設定されます。',
]));

c.push(pageBreak());

c.push(h1('4. Driveフォルダの準備'));
c.push(p('Driveに以下の5つのフォルダを作成します。配下の階層はシステムが自動生成するため、作成する必要はありません。'));
c.push(table(
  ['フォルダ', '用途 / 自動生成される配下の構成'],
  [
    ['見積書ルート', '{期}期 / 未請求案件 / {案件番号}_{案件名}'],
    ['請求書ルート', '{期}期 / 未請求案件・請求中案件 / {案件番号}_{案件名}'],
    ['納品書ルート', '{期}期 / {案件番号}_{案件名}'],
    ['アーカイブフォルダ', '過去期のデータファイル「案件データ_{期}期」が保存される'],
    ['バックアップフォルダ', '日次バックアップ「バックアップ_{yyyyMMdd}」が保存される'],
  ],
  [2600, 6400]
));
c.push(spacer());
c.push(p('作成後、各フォルダのIDを控えてください。フォルダIDはURLの「/folders/」以降の文字列です。'));
c.push(p('例: https://drive.google.com/drive/folders/1AbCdEfGhIjKlMnOpQrStUvWxYz → 1AbCdEfGhIjKlMnOpQrStUvWxYz', { italics: true }));

c.push(h1('5. テンプレートファイルの準備'));
c.push(p('見積書・請求書・納品書のテンプレートをスプレッドシートとして作成し、Driveに配置します。'));

c.push(h2('5.1 転記先セルの配置'));
c.push(p('システムは以下のセルへ自動転記します。テンプレートのレイアウトを変更する場合は、スクリプトのConstants.gsも合わせて修正が必要です。'));
c.push(table(
  ['項目', '見積書', '請求書', '納品書'],
  [
    ['郵便番号', 'B5', 'B5', 'B5'],
    ['住所1 / 住所2', 'B6 / B7', 'B6 / B7', 'B6 / B7'],
    ['社名', 'B8', 'B8', 'B8'],
    ['部署 / 担当者名', 'B10 / B11', 'B10 / B11', 'B10 / B11'],
    ['案件番号', 'G1', 'G1', 'G1'],
    ['通し番号', 'G6', 'G6', 'G6'],
    ['社印の基準セル', 'G4', 'G4', 'G4'],
    ['担当社員名 / メール', 'G12 / G13', 'G12 / G13', 'G12 / G13'],
    ['件名', 'C16', 'C16', 'C16'],
    ['見積書No.参照', '—', 'G53', 'G52'],
    ['請求書No.参照', '—', '—', 'G53'],
    ['作成者 / 作成日時', 'K56 / L56', 'C56 / D56', 'C56 / D56'],
    ['承認者 / 承認日時', 'K57 / L57', 'C57 / D57', '—'],
    ['PDF出力者 / 日時', 'K58 / L58', 'C58 / D58', 'C57 / D57'],
  ],
  [2400, 2200, 2200, 2200]
));
c.push(spacer());
c.push(p('見積書のみ、取引先の請求条件を転記する専用セル（O7・O8・O10・O11・O13・O14）があります。'));

c.push(h2('5.2 PDF出力のページ範囲'));
c.push(p('PDFは以下のセル範囲を、それぞれ1ページに収めて出力します。テンプレートのレイアウトはこの範囲に収まるよう設計してください。'));
c.push(table(
  ['書類種別', '1ページ目', '2ページ目', '3ページ目'],
  [
    ['見積書', 'A1:H54', 'I1:P54', 'I55:P58'],
    ['請求書', 'A1:H54', 'A55:H58', '—'],
    ['納品書', 'A1:H54', 'A55:H58', '—'],
  ],
  [1800, 2400, 2400, 2400]
));
c.push(spacer());
c.push(box('ページ区切りの設定は不要です', [
  'Googleスプレッドシートには手動でページ区切りを挿入する機能がありませんが、',
  'システム側でページ範囲を制御しているため、テンプレート側の設定は不要です。',
  'ページ範囲を変更したい場合は、スクリプトのConstants.gsにあるPDF_PAGE_RANGESを修正してください。',
]));

c.push(h2('5.3 配置とID取得'));
c.push(p('作成した3つのテンプレートをDriveに配置し、それぞれのファイルIDを控えてください。ファイルIDはURLの「/d/」と「/edit」の間の文字列です。'));

c.push(pageBreak());

c.push(h1('6. 社印画像の準備'));
c.push(table(
  ['項目', '要件'],
  [
    ['ファイル形式', 'PNGまたはJPEG（Googleスライド・図形描画は使用不可）'],
    ['推奨サイズ', '300 × 300 px 程度、1MB以下'],
    ['サイズ上限', '2MB以下かつ100万画素以下（超過時は縮小版が自動使用されます）'],
    ['配置場所', 'Driveの任意の場所'],
    ['共有設定', '一般公開は不要。利用者が閲覧できる権限があればよい'],
    ['表示サイズ', '150 × 150 px（Constants.gsで変更可能）'],
  ],
  [2200, 6800]
));
c.push(spacer());
c.push(p('配置後、そのファイルの共有リンク（https://drive.google.com/file/d/xxxxx/view?usp=sharing 形式）をそのまま控えてください。URLの加工は不要です。'));

c.push(h1('7. スクリプトの配置'));
c.push(p('メインスプレッドシートのメニュー「拡張機能」→「Apps Script」からスクリプトエディタを開き、以下のファイルを配置します。'));
c.push(table(
  ['ファイル', '役割'],
  [
    ['Config.gs / Constants.gs / DocTypes.gs', '設定値・固定値・書類種別ごとの差分'],
    ['Utils.gs / LogService.gs', '共通処理・排他制御・操作ログ'],
    ['PeriodService.gs', '期の管理とフォルダ・シート生成'],
    ['CaseService.gs / CaseRestoreService.gs / SetupService.gs', '案件行の操作・自動採番・案件中止・中止の取り消し・シート構成の修復'],
    ['ClientService.gs / RoleService.gs', '取引先DB・社員DBの参照'],
    ['DocumentService.gs / TemplateFillService.gs', '書類ファイル操作・PDF出力・転記・社印・保護'],
    ['ApprovalService.gs / DeliveryService.gs / FinalApprovalService.gs', '承認フロー・納品書・最終承認'],
    ['NotificationService.gs / ReportService.gs', '通知・定期レポート・リマインド'],
    ['QuoteInvoiceDiffService.gs', '見積書と請求書の差分ハイライト'],
    ['WebAppEntry.gs / AdminProxyService.gs', 'Webアプリの入り口と、管理用アカウント権限での実行'],
    ['TestDataResetService.gs', 'テストデータのリセット（管理者が手動実行）'],
    ['ArchiveService.gs / BackupService.gs', '過去期アーカイブ・日次バックアップ'],
    ['SidebarController.gs / Triggers.gs', 'サイドバーの入り口・トリガー・メニュー'],
    ['Sidebar.html / JavaScript.html / Stylesheet.html', 'サイドバーの画面'],
  ],
  [4000, 5000]
));
c.push(spacer());
c.push(p('プロジェクト設定でタイムゾーンが「Asia/Tokyo」、ランタイムが「V8」になっていることを確認してください。'));

c.push(h1('8. Webアプリのデプロイ'));
c.push(p('本システムは、サイドバーからの書き込み操作をすべて管理用アカウントの権限で実行します。そのために、同じスクリプトをWebアプリとしてデプロイし、書き込み処理はそのWebアプリを経由して実行する構成になっています。'));
c.push(spacer());
c.push(box('なぜ必要か', [
  'サイドバーからの操作は、通常は「ボタンを押した本人」の権限で実行されます。そのため、',
  '案件シートの自動書き込み列を保護するとスクリプトからの書き込みまでブロックされ、',
  'また作成される書類ファイルのオーナーが操作した本人になってしまいます。',
  'Webアプリを「実行ユーザー: 自分」でデプロイし、そこを経由させることで解決しています。',
]));
c.push(spacer());
c.push(p('必ず管理用アカウントでログインした状態で、以下を実行してください。'));
[
  'スクリプトエディタ右上の「デプロイ」→「新しいデプロイ」をクリックします',
  '種類の選択で「ウェブアプリ」を選びます',
  '「次のユーザーとして実行」を「自分（管理用アカウントのアドレス）」にします',
  '「アクセスできるユーザー」を「全員」にします',
  '「デプロイ」をクリックし、表示されたウェブアプリのURL（.../exec で終わるもの）をコピーします',
].forEach(t => c.push(step(t)));
c.push(spacer());
c.push(box('「アクセスできるユーザー: 全員」について', [
  '本来は「組織内の全員」が適切ですが、Google Workspaceを契約していない環境では',
  'ドメインの所属を判定できず、正常に動作しません（HTTP 404になります）。',
  'そのため「全員」を指定しています。URLは推測できないランダムな文字列であり、',
  'また実際に操作できるかどうかはスクリプト側のロール判定で別途制御されるため、',
  'この設定でも権限のない操作は実行できません。',
]));
c.push(spacer());
c.push(box('コードを更新したら、必ず再デプロイしてください', [
  '書き込み操作はWebアプリ経由で実行されるため、スクリプトを更新しただけでは反映されません。',
  '「デプロイ」→「デプロイを管理」から既存のデプロイを選び、鉛筆アイコン →',
  'バージョンを「新バージョン」にして「デプロイ」すると、URLを変えずに更新できます。',
  '新しいデプロイを作成してURLが変わった場合は、第9章のWEBAPP_URLを再設定してください。',
], 'warn'));

c.push(pageBreak());

c.push(h1('9. スクリプトプロパティの設定'));
c.push(p('スクリプトエディタの「プロジェクトの設定」→「スクリプト プロパティ」で、以下を設定します。'));
c.push(table(
  ['プロパティ名', '設定値', '必須'],
  [
    ['MAIN_SPREADSHEET_ID', 'メインスプレッドシートのID', '任意'],
    ['SHEET_NAME_CLIENT_DB', '取引先DBのシート名', '必須'],
    ['SHEET_NAME_STAFF_DB', '社員DBのシート名', '必須'],
    ['FOLDER_ID_QUOTE_ROOT', '見積書ルートフォルダのID', '必須'],
    ['FOLDER_ID_INVOICE_ROOT', '請求書ルートフォルダのID', '必須'],
    ['FOLDER_ID_DELIVERY_ROOT', '納品書ルートフォルダのID', '必須'],
    ['FOLDER_ID_ARCHIVE_ROOT', 'アーカイブフォルダのID', '必須'],
    ['FOLDER_ID_BACKUP_ROOT', 'バックアップフォルダのID', '必須'],
    ['QUOTE_TEMPLATE_FILE_ID', '見積書テンプレートのファイルID', '必須'],
    ['INVOICE_TEMPLATE_FILE_ID', '請求書テンプレートのファイルID', '必須'],
    ['DELIVERY_TEMPLATE_FILE_ID', '納品書テンプレートのファイルID', '必須'],
    ['COMPANY_SEAL_IMAGE_URL', '社印画像の共有リンク（URLのまま）', '必須'],
    ['ADMIN_TRIGGER_ACCOUNT_EMAIL', '管理用アカウントのメールアドレス\n（書類作成の実行アカウントの検証に使用）', '必須'],
    ['CHATWORK_API_TOKEN', 'Chatwork APIトークン', '任意'],
    ['WEBAPP_URL', '第8章でコピーしたウェブアプリのURL\n（.../exec で終わるもの）', '必須'],
  ],
  [3400, 4400, 1200]
));
c.push(spacer());
c.push(box('補足', [
  '・MAIN_SPREADSHEET_IDは、実装上はバインド先のスプレッドシートを直接参照するため未設定でも動作します。',
  '・CHATWORK_API_TOKENは、総務以外の社員へChatwork通知を行う場合に必要です。',
  '・ADMIN_TRIGGER_ACCOUNT_EMAILが未設定の場合、書類作成時の実行アカウント検証が行われず、',
  '　管理用アカウント以外で書類が作成されても気付けません。必ず設定してください。',
  '・FOLDER_ID_ARCHIVE_ROOT / FOLDER_ID_BACKUP_ROOT が未設定の場合、日次処理の',
  '　アーカイブ・バックアップがエラーになります（他の日次処理は継続します）。',
  '・WEBAPP_URLが未設定または誤っていると、サイドバーの書き込み操作がすべて失敗します。',
  '　メニュー「管理者用メニュー > WebアプリのURLを設定する」からも設定できます。',
]));

c.push(pageBreak());

c.push(h1('10. トリガーの設定と認可'));

c.push(h2('10.1 管理者による設定（1回のみ）'));
c.push(p('管理用アカウントでログインした状態で、以下を実行します。'));
[
  'スクリプトエディタを開きます',
  '関数の選択欄で「installTriggers」を選び、「実行」をクリックします',
  'Googleの認可画面が表示されるので、内容を確認して許可します',
  '実行が完了すると、以下4つのトリガーが登録されます',
].forEach(t => c.push(step(t)));
c.push(spacer());
c.push(table(
  ['トリガー', '実行タイミング', '役割'],
  [
    ['onEditInstallable', 'シート編集時', '案件番号の自動採番、変更記録、案件番号順への整列'],
    ['dailyScheduledTasks', '毎日 0:30頃', '翌期の準備、選択肢更新、アーカイブ、バックアップ、請求予定レポート'],
    ['morningScheduledTasks', '毎日 10:00頃', '未承認案件レポート、請求漏れリマインド、放置書類アラート'],
    ['onOpenInstallable', 'シートを開いた時', '実行者本人のサイドバー自動表示'],
  ],
  [2400, 1900, 4700]
));
c.push(spacer());
c.push(box('installTriggersは必ず1名だけが実行すること', [
  'インストール型トリガーは登録したユーザー個人に紐づき、登録者の数だけ実行されます。',
  '複数人がinstallTriggersを実行すると、1回の編集で人数分の採番処理が走り、',
  '請求予定レポートやバックアップも人数分重複して実行されます。',
  '自動採番と日次処理のトリガーは、必ず管理用アカウント1名のみが登録してください。',
], 'warn'));

c.push(h2('10.2 各利用者による設定（利用者ごとに1回）'));
c.push(p('サイドバーの自動表示は利用者ごとに設定が必要です。スクリプトエディタを開く必要はありません。'));
[
  'メインスプレッドシートを開きます',
  'メニュー「案件管理システム」→「サイドバー自動表示を有効にする」をクリックします',
  '認可画面が表示されたら許可します',
].forEach(t => c.push(step(t)));

c.push(h2('10.3 スコープ追加時の再認可'));
c.push(p('スクリプトを更新して新しいGoogleサービスを使用するようになった場合、再認可が必要です。認可が完了していない状態では、単純トリガーはエラーも出さずスキップされます。サイドバーが自動表示されない場合は、スクリプトエディタから任意の関数を1度手動実行し、認可ダイアログを完了させてください。'));

c.push(h1('11. 動作確認チェックリスト'));
c.push(p('設定完了後、以下の順に確認してください。詳細な受け入れテストは別紙「ユーザーテスト チェックリスト」を使用してください。'));
c.push(table(
  ['#', '確認内容', '期待される結果'],
  [
    ['1', 'メニュー「案件シートの構成を確認・修復する」を実行', '全シートが「変更なし（正常）」または修復完了と表示される（列数36・見出しは表示=2行目/DB=1行目）'],
    ['2', 'メニュー「社印設定を確認する」を実行', '「正常」と表示され、画像の種類とサイズが表示される'],
    ['3', 'メニュー「今すぐバックアップを作成する」を実行', 'バックアップフォルダにファイルが作成される'],
    ['4', 'スプレッドシートを開き直す', 'サイドバーが自動的に表示される'],
    ['5', '案件の5項目を入力', '案件番号が自動採番される'],
    ['6', '「見積書作成」を実行', '書類が生成され、情報が転記されている'],
    ['7', '書類ファイルのオーナーを確認', '一般社員が操作した場合も管理用アカウントになっている'],
    ['7-2', '書類の「ファイル > ダウンロード」を確認', '閲覧権限の利用者には表示されない'],
    ['8', '「見積書完成」を実行', '総務へHTML形式の承認依頼メールが届く'],
    ['9', '「差し戻し」を実行', 'シートがロックされ、「完成」が押せず「再作成」が押せる'],
    ['10', '「再作成」を実行', '同一ファイル内に新シートが作られ、旧シートが退避される'],
    ['11', '「見積書承認」を実行', '社印が押印され、シートがロックされる'],
    ['12', '「見積書出力(PDF)」を実行', 'PDFが1ページ（A1:H54）で生成され、履歴欄にPDF出力が記録されている'],
    ['13', '再作成した直後にPDF出力ボタンを確認', '再承認されるまで押せない状態になっている'],
    ['14', '請求書を作成し、明細を書き換える', '見積書と違うセルの背景が黄色になる。戻すと消える'],
    ['15', '請求書・納品書も同様に確認', '通し番号の相互参照が記載されている'],
    ['16', '最終承認または案件中止を実行', '表示シートから消え、全案件DBには残る。中止時は書類が中止案件フォルダへ移動する'],
    ['17', '全案件DBで行を選び「元に戻す」を実行', '表示シートへ戻り、案件番号順に並ぶ'],
    ['18', '「操作ログ」シートを確認', 'これまでの全操作が、操作した本人の名前で記録されている'],
    ['19', 'エディタで runScheduledNotificationsForTest を実行', '定期通知3種が送信される（送信日を待たずに確認できる）'],
  ],
  [500, 3500, 5000]
));

c.push(pageBreak());

c.push(h1('12. 運用・保守'));

c.push(h2('12.1 期の切り替え'));
c.push(p('期は毎年5月1日に切り替わります。切り替えの1ヶ月前（4月1日）に日次トリガーが動作し、翌期のシートとフォルダを自動生成します。'));
[
  '「原本_表示」「原本_全案件DB」をコピーして「{期}期_表示」「{期}期_全案件DB」が作成される',
  '各書類ルートフォルダの配下に「{期}期」フォルダとサブフォルダが作成される',
  '4月1日に日次トリガーが動作しなかった場合は、ensureNextPeriodResourcesIfNeeded関数を手動実行する',
].forEach(t => c.push(bullet(t)));

c.push(h2('12.2 過去期データのアーカイブ'));
c.push(p('メインスプレッドシートが肥大化しないよう、期が切り替わったら前期以前のシートは別ファイルへ自動的に退避されます。'));
c.push(table(
  ['項目', '内容'],
  [
    ['対象', '当期より前の「{n}期_表示」「{n}期_全案件DB」'],
    ['退避先', 'アーカイブフォルダ内の「案件データ_{n}期」'],
    ['実行契機', '日次トリガー（自動）／メニュー「過去期のデータをアーカイブする」（手動）'],
    ['安全策1', 'コピー後に行数・列数の一致を検証し、一致した場合のみ元シートを削除する'],
    ['安全策2', '表示シートに案件が残っている期は自動アーカイブせず、操作ログに記録する'],
  ],
  [2200, 6800]
));
c.push(spacer());
c.push(box('アーカイブ後は、その期の案件をサイドバーから参照できなくなります', [
  'サイドバーは当期のシートのみを対象とするため、アーカイブ済みの期の案件は操作も履歴閲覧も',
  'できなくなります。過去案件を確認する場合はアーカイブファイルを直接開いてください。',
  '',
  '期をまたいで進行中の案件がある場合は自動アーカイブが見送られます。案件を完了または中止すれば',
  '翌日以降の自動処理で退避されます。急ぐ場合はメニューから手動実行してください',
  '（確認ダイアログに残案件数が表示されます）。',
], 'warn'));

c.push(h2('12.3 バックアップ'));
c.push(table(
  ['項目', '内容'],
  [
    ['対象', '当期の「{期}期_全案件DB」と「取引先DB」'],
    ['保存先', 'バックアップフォルダ内の「バックアップ_{yyyyMMdd}」'],
    ['実行契機', '日次トリガー（自動）／メニュー「今すぐバックアップを作成する」（手動）'],
    ['重複防止', '同日中に既にバックアップがある場合は作成をスキップする'],
    ['保持世代数', '30世代（Constants.gs の BACKUP_GENERATIONS で変更可能）'],
    ['削除方法', 'ゴミ箱へ移動（完全削除ではないため、誤操作時も復旧できる）'],
  ],
  [2200, 6800]
));
c.push(spacer());
c.push(p('復旧が必要な場合は、該当日のバックアップファイルを開き、必要な範囲をコピーして元のシートへ戻してください。'));

c.push(h2('12.4 日次処理の内容と順序'));
c.push(table(
  ['実行順', '処理', '実行される条件'],
  [
    ['1', '翌期リソースの準備', '期切替の1ヶ月前（4/1）'],
    ['2', '終了予定・請求予定の選択肢更新', '毎日（configシートとD/E列のプルダウン・今月ハイライトを更新）'],
    ['3', '過去期データのアーカイブ', '当期より前のシートが残っている場合'],
    ['4', 'データベースのバックアップ', '当日分が未作成の場合'],
    ['5', '請求予定レポートの送信', '前月20日・当月5日'],
  ],
  [1000, 4000, 4000]
));
c.push(spacer());
c.push(p('1つの処理が失敗しても後続は継続されます。失敗した処理は「操作ログ」シートにエラーとして記録されます。'));

c.push(h2('12.5 設定値の変更箇所'));
c.push(table(
  ['変更したい内容', '変更箇所'],
  [
    ['社印のサイズ', 'Constants.gs の SEAL_IMAGE_WIDTH_PX / SEAL_IMAGE_HEIGHT_PX'],
    ['社印の横位置', 'Constants.gs の SEAL_IMAGE_OFFSET_X_PX（右へずらす場合は値を増やす）'],
    ['社印の縦位置', 'Constants.gs の SEAL_IMAGE_BOTTOM_ROW（画像の下端を合わせる行。既定13）\n行の高さから自動計算するため、下げたい場合はこの行番号を増やす'],
    ['社印の基準セル', '各 *_TEMPLATE_CELLS の SEAL_IMAGE_RANGE'],
    ['社印の画像', 'スクリプトプロパティ COMPANY_SEAL_IMAGE_URL'],
    ['PDFのページ範囲', 'Constants.gs の PDF_PAGE_RANGES'],
    ['テンプレートのセル配置', 'Constants.gs の各 *_TEMPLATE_CELLS'],
    ['バックアップの保持世代数', 'Constants.gs の BACKUP_GENERATIONS'],
    ['終了予定・請求予定の月範囲', 'Constants.gs の SCHEDULE_OPTIONS_MONTHS_BEFORE / SCHEDULE_OPTIONS_MONTHS_AFTER'],
    ['終了予定・請求予定の固定選択肢', 'Constants.gs の SCHEDULE_OPTIONS_FIXED_CHOICES（既定:「未定」「営業案件」）'],
    ['今月請求ハイライトの色', 'Constants.gs の CURRENT_MONTH_BILLING_TEXT_COLOR'],
    ['ロール名', 'Constants.gs の ROLES および社員DBの記載'],
    ['請求予定レポートの送信日', 'ReportService.gs の sendBillingSummaryReportIfNeeded_'],
    ['未承認案件レポートの送信日', 'ReportService.gs の UNAPPROVED_REPORT_DAY（既定25日）'],
    ['請求漏れリマインドの送信日', 'ReportService.gs の OVERDUE_BILLING_REMINDER_DAY（既定5日）'],
    ['放置書類アラートの日数', 'ReportService.gs の STALLED_DOCUMENT_ALERT_DAYS（既定7日）'],
    ['朝の通知の送信時刻', 'Triggers.gs の installTriggers 内 morningScheduledTasks の atHour（既定10時）'],
    ['差分ハイライトの対象セル', 'Constants.gs の QUOTE_INVOICE_DIFF_RANGES'],
    ['差分ハイライトの色', 'Constants.gs の QUOTE_INVOICE_DIFF_COLOR'],
    ['書類作成時に編集を許す範囲', 'Constants.gs の DOC_EDITABLE_RANGES（書類種別ごと）'],
    ['見積書で常に編集を許す範囲', 'Constants.gs の QUOTE_ALWAYS_EDITABLE_RANGES（既定 J22:O47 / N53）'],
    ['ロック時に表示する説明', 'Constants.gs の DOC_LOCK_MESSAGE'],
  ],
  [2800, 6200]
));

c.push(h2('12.6 監査・履歴の確認'));
c.push(p('「操作ログ」シートに全操作が記録されます。このシートは期をまたいで永続し、システムが自動生成・追記します。手動で行を削除しないでください。'));
c.push(p('なお、操作ログは追記され続けるため、運用年数に応じて肥大化します。将来的には年次での分割が必要になる可能性があります。'));

c.push(pageBreak());

c.push(h1('13. トラブルシューティング'));
c.push(box('メニューが全く表示されない場合はまずこれを確認', [
  '単純トリガー（onOpen）は、未認可のスコープを要する処理を含むと、エラーも出さず',
  '「関数全体の実行」がスキップされる。メニュー作成もこの中に含まれるため、原因不明のまま',
  '「メニューが一切出ない」という症状になる（第12章12.5参照）。',
  '',
  'また、スクリプトを更新しても、既に開いているタブでは onOpen は再実行されない。',
  '対処: (1) スクリプトエディタから installTriggers 等の関数を1度手動実行し認可を完了させる',
  '(2) スプレッドシートのタブを閉じて開き直す。',
], 'warn'));
c.push(spacer());
c.push(table(
  ['症状', '原因と対処'],
  [
    ['メニューが一切表示されない',
     '未認可、またはタブを開いたままスクリプトを更新した可能性が高い。上のボックスの手順を実施する'],
    ['サイドバーが自動表示されない',
     '認可が完了していない可能性がある。スクリプトエディタから任意の関数を1度手動実行し、\n認可ダイアログを完了させる。その後メニューから自動表示を有効にし直す'],
    ['終了予定・請求予定のプルダウンが選べない',
     'configシートがまだ生成されていない（初回の日次トリガー実行前）か、日次トリガーが\n未登録の可能性がある。操作ログを確認し、installTriggersが実行済みか確認する'],
    ['今月のはずの請求予定が青文字にならない',
     'セルの値が「YYYY/MM 前半」「YYYY/MM 後半」の形式と完全一致しているか確認する\n（自動生成された選択肢からではなく直接入力した場合にずれることがある）'],
    ['社印が押印されない',
     'メニュー「社印設定を確認する」で診断する。画像形式が不正、サイズ超過、\nまたは閲覧権限が無い可能性がある。「操作ログ」シートのエラー行も確認する'],
    ['承認済み書類を編集できてしまう',
     '管理用アカウント以外で書類が作成された可能性がある。操作ログの警告を確認する。\nなお、管理用アカウント自身は仕様上どうしても編集できてしまう'],
    ['「範囲の座標が...」等の範囲外エラー',
     'シートの列数が不足している。メニュー「案件シートの構成を確認・修復する」を実行する'],
    ['アーカイブが実行されない',
     '表示シートに案件が残っている期は自動アーカイブされない（仕様）。操作ログを確認し、\n必要ならメニューから手動実行する。FOLDER_ID_ARCHIVE_ROOT の設定も確認する'],
    ['バックアップが作成されない',
     'FOLDER_ID_BACKUP_ROOT が未設定か、当日分が既に作成済みの可能性がある。\nバックアップフォルダと操作ログを確認する'],
    ['請求予定レポートが重複して届く',
     '複数人がinstallTriggersを実行している。管理者以外のonEdit・日次トリガーを削除する'],
    ['案件番号が採番されない',
     '5項目すべてが入力されているか確認する。またinstallTriggersが実行済みか確認する'],
    ['PDFの内容が切れる',
     'テンプレートのレイアウトがPDF_PAGE_RANGES（既定 A1:H54）に収まっているか確認する'],
    ['書類からダウンロード・印刷できない',
     '仕様。PDF化はサイドバーの「PDF出力」に一本化している。\n閲覧権限の利用者に対してのみ制限され、オーナーは従来どおり操作できる'],
    ['「スクリプトプロパティが設定されていません」',
     '第9章の必須プロパティが設定されているか確認する'],
    ['サイドバーの操作がすべて失敗する\n（想定外の応答・HTTP 404など）',
     'WEBAPP_URLが未設定か、デプロイのURLと一致していない。第8章の手順でデプロイし直し、\nメニュー「管理者用メニュー > WebアプリのURLを設定する」でURLを再設定する'],
    ['コードを更新したのに動作が変わらない',
     'Webアプリの再デプロイを忘れている。書き込み操作はWebアプリ経由で実行されるため、\n「デプロイを管理」から新バージョンをデプロイする必要がある（第8章）'],
    ['「保護されているセルやオブジェクトを編集しようとしています」',
     'Webアプリを経由せずに実行されている。WEBAPP_URLの設定と再デプロイを確認する'],
    ['請求書の差分ハイライト（黄色）が付かない',
     '請求書を作成・再作成し直すと設定される。既存の請求書には遡って適用されない'],
    ['ボタンを押しても何も起きない',
     'サイドバー下部のメッセージを確認する。「案件が選択されていません」の場合はシート上で\n案件の行をクリックする。何も表示されない場合はボタンが非活性（ツールチップに理由が出る）'],
    ['書類の作成が二重に実行された',
     '操作ログを「二重実行」で検索する。防止機能により2件目は弾かれるが、\n発生した場合は日時・案件番号・操作種別を控えて開発担当者へ連絡する'],
    ['案件番号が重複した',
     'メニュー「案件シートの構成を確認・修復する」を実行後、operationログを確認する。\nまたinstallTriggersを複数人が実行していないか、トリガー一覧で確認する'],
    ['「他のユーザーが処理中です」',
     '排他制御による正常な動作。しばらく待って再実行する'],
    ['取引先・社員が見つからないエラー',
     '案件シートの「取引先名」「担当」が、各DBの記載と完全一致しているか確認する'],
  ],
  [2600, 6400]
));

c.push(spacer());
c.push(box('エラー発生時の調査手順', [
  '1. 「操作ログ」シートで、該当時刻のエラー行と詳細を確認する',
  '2. スクリプトエディタの「実行数」画面で、実行ログとエラー内容を確認する',
  '3. エラーコードが表示された場合は、その番号を控えて開発担当者へ連絡する',
]));

module.exports = () => C.buildDoc(c);
